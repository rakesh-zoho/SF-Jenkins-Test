# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: lead-creation.spec.js >> Lead Creation >> Create Lead with Required Fields Only
- Location: tests\lead-creation.spec.js:53:3

# Error details

```
Error: locator.click: Target page, context or browser has been closed
```

# Test source

```ts
  1   | import { test, expect } from '@playwright/test';
  2   | import 'dotenv/config';
  3   | import { waitForSFLoad, switchToAllRecords, navigateToApp } from '../utils/sf-helpers.js';
  4   | import { captureScreenshot, sfStep, setAllureMeta } from '../utils/reporter-utils.js';
  5   | 
  6   | // Reuse the saved Salesforce auth session
  7   | test.use({ storageState: './reports/.auth-state.json' });
  8   | 
  9   | /**
  10  |  * Extended test fixture: sfPage
  11  |  * Provides a pre-authenticated Salesforce Lightning page.
  12  |  */
  13  | const sfTest = test.extend({
  14  |   sfPage: async ({ page }, use) => {
  15  |     await page.goto(process.env.SF_URL);
  16  |     await waitForSFLoad(page);
  17  |     await use(page);
  18  |   },
  19  | });
  20  | 
  21  | /**
  22  |  * Lead Creation Test Suite
  23  |  * Epic: CRM
  24  |  * Feature: Lead Management
  25  |  * Story: Create Lead
  26  |  * Severity: Critical
  27  |  *
  28  |  * spec: specs/lead-creation-plan.md
  29  |  * seed: tests/seed.spec.js
  30  |  */
  31  | test.describe('Lead Creation', () => {
  32  |   test.beforeEach(async () => {
  33  |     // Set Allure metadata for all tests in this suite
  34  |     await setAllureMeta({
  35  |       epic: 'CRM',
  36  |       feature: 'Lead Management',
  37  |       story: 'Create Lead',
  38  |       severity: 'critical',
  39  |     });
  40  |   });
  41  | 
  42  |   test.afterEach(async ({ page }, testInfo) => {
  43  |     // Screenshot on failure
  44  |     if (testInfo.status !== 'passed') {
  45  |       await captureScreenshot(page, `failure-${testInfo.title.replace(/\s+/g, '-')}`);
  46  |     }
  47  |   });
  48  | 
  49  |   /**
  50  |    * Test 1.1: Create Lead with Required Fields Only
  51  |    * Verify that a Lead can be created with only the required fields filled in.
  52  |    */
  53  |   sfTest('Create Lead with Required Fields Only', async ({ sfPage: page }) => {
  54  |     // 1. Navigate to Leads app
  55  |     await sfStep('Navigate to Leads app', page, async () => {
  56  |       await navigateToApp(page, 'Leads');
  57  |     });
  58  | 
  59  |     // 2. Click the "New" button on the Leads list view
  60  |     await sfStep('Click New button', page, async () => {
> 61  |       await page.getByRole('button', { name: /^New$/i }).click();
      |                                                          ^ Error: locator.click: Target page, context or browser has been closed
  62  |       await waitForSFLoad(page);
  63  |       await expect(page.locator('form')).toBeVisible({ timeout: 10000 });
  64  |     });
  65  | 
  66  |     // 3. Enter "John" in the First Name field
  67  |     await sfStep('Enter First Name: John', page, async () => {
  68  |       const firstNameInput = page.getByLabel(/First Name/i);
  69  |       await expect(firstNameInput).toBeVisible();
  70  |       await firstNameInput.fill('John');
  71  |       await expect(firstNameInput).toHaveValue('John');
  72  |     });
  73  | 
  74  |     // 4. Enter "Doe" in the Last Name field
  75  |     await sfStep('Enter Last Name: Doe', page, async () => {
  76  |       const lastNameInput = page.getByLabel(/Last Name/i);
  77  |       await expect(lastNameInput).toBeVisible();
  78  |       await lastNameInput.fill('Doe');
  79  |       await expect(lastNameInput).toHaveValue('Doe');
  80  |     });
  81  | 
  82  |     // 5. Enter "Acme Corporation" in the Company field
  83  |     await sfStep('Enter Company: Acme Corporation', page, async () => {
  84  |       const companyInput = page.getByLabel(/Company/i);
  85  |       await expect(companyInput).toBeVisible();
  86  |       await companyInput.fill('Acme Corporation');
  87  |       await expect(companyInput).toHaveValue('Acme Corporation');
  88  |     });
  89  | 
  90  |     // 6. Click the "Save" button
  91  |     await sfStep('Click Save button', page, async () => {
  92  |       await page.getByRole('button', { name: /^Save$/i }).click();
  93  |       await waitForSFLoad(page);
  94  |     });
  95  | 
  96  |     // Verify Lead record is created successfully
  97  |     await sfStep('Verify Lead created with correct details', page, async () => {
  98  |       // Should be redirected to the newly created Lead detail view
  99  |       await expect(page).toHaveURL(/lightning\/r\/Lead\//);
  100 | 
  101 |       // Verify fields display with correct information
  102 |       await expect(page.getByText('John')).toBeVisible();
  103 |       await expect(page.getByText('Doe')).toBeVisible();
  104 |       await expect(page.getByText('Acme Corporation')).toBeVisible();
  105 | 
  106 |       // Success message should appear (Salesforce typically shows "Saved" message)
  107 |       const successMsg = page.locator('.slds-notify__content, [role="alert"]');
  108 |       await expect(successMsg).toBeVisible({ timeout: 5000 });
  109 |     });
  110 | 
  111 |     // Verify Lead ID is generated and visible
  112 |     await sfStep('Verify Lead ID is generated', page, async () => {
  113 |       const leadId = page.locator('[data-test-id*="Lead-"], .record-id');
  114 |       await expect(leadId.or(page.locator('span:has-text("00Q")'))).toBeVisible({ timeout: 5000 });
  115 |     });
  116 |   });
  117 | 
  118 |   /**
  119 |    * Test 1.2: Create Lead with All Standard Fields
  120 |    * Verify that a Lead can be created with all standard fields populated.
  121 |    */
  122 |   sfTest('Create Lead with All Standard Fields', async ({ sfPage: page }) => {
  123 |     // 1. Navigate to Leads app
  124 |     await sfStep('Navigate to Leads app', page, async () => {
  125 |       await navigateToApp(page, 'Leads');
  126 |     });
  127 | 
  128 |     // 2. Click the "New" button on the Leads list view
  129 |     await sfStep('Click New button', page, async () => {
  130 |       await page.getByRole('button', { name: /^New$/i }).click();
  131 |       await waitForSFLoad(page);
  132 |       await expect(page.locator('form')).toBeVisible({ timeout: 10000 });
  133 |     });
  134 | 
  135 |     // 3. Enter "Jane" in the First Name field
  136 |     await sfStep('Enter First Name: Jane', page, async () => {
  137 |       const firstNameInput = page.getByLabel(/First Name/i);
  138 |       await expect(firstNameInput).toBeVisible();
  139 |       await firstNameInput.fill('Jane');
  140 |       await expect(firstNameInput).toHaveValue('Jane');
  141 |     });
  142 | 
  143 |     // 4. Enter "Smith" in the Last Name field
  144 |     await sfStep('Enter Last Name: Smith', page, async () => {
  145 |       const lastNameInput = page.getByLabel(/Last Name/i);
  146 |       await expect(lastNameInput).toBeVisible();
  147 |       await lastNameInput.fill('Smith');
  148 |       await expect(lastNameInput).toHaveValue('Smith');
  149 |     });
  150 | 
  151 |     // 5. Enter "Tech Innovations Inc" in the Company field
  152 |     await sfStep('Enter Company: Tech Innovations Inc', page, async () => {
  153 |       const companyInput = page.getByLabel(/Company/i);
  154 |       await expect(companyInput).toBeVisible();
  155 |       await companyInput.fill('Tech Innovations Inc');
  156 |       await expect(companyInput).toHaveValue('Tech Innovations Inc');
  157 |     });
  158 | 
  159 |     // 6. Enter "Manager" in the Title field
  160 |     await sfStep('Enter Title: Manager', page, async () => {
  161 |       const titleInput = page.getByLabel(/Title/i);
```